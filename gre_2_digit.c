#include<stdio.h>

int main()
 {
  int a,b;
  printf("Enter two no: ");
  scanf("%d%d",&a,&b);

  if(a>b)
   printf("%d is greater then b",a);

  else
   printf("%d is greater then a",b);

   return 0;
  }
